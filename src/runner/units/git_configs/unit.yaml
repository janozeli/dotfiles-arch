id: git_configs
name: "Git workspace configs"
stages:
  - name: verify
    tasks:
      - task: verify.sh
  - name: setup
    tasks:
      - task: setup.sh
        depends_on:
          - directories
          - packages

actions:
  teardown:
    task: teardown.sh
    description: "Reverte este unit"
