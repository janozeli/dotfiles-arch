id: mise
name: "mise + Node/uv/pnpm"
stages:
  - name: verify
    tasks:
      - task: verify.sh
  - name: setup
    tasks:
      - task: setup.sh
    executor:
      timeout: 300

actions:
  teardown:
    task: teardown.sh
    description: "Reverte este unit"
