id: packages
name: "Pacotes (yay)"
critical: true
stages:
  - name: verify
    tasks:
      - task: verify.sh
  - name: setup
    tasks:
      - task: setup.sh
        depends_on:
          - yay
    executor:
      timeout: 600

actions:
  teardown:
    task: teardown.sh
    description: "Reverte este unit"
