id: gpu_drivers
name: "GPU drivers"
stages:
  - name: verify
    tasks:
      - task: verify.sh
  - name: setup
    tasks:
      - task: setup.sh
        depends_on:
          - packages
    executor:
      timeout: 300

actions:
  teardown:
    task: teardown.sh
    description: "Reverte este unit"
