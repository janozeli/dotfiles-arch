# dotfiles-arch

Dotfiles para Arch Linux + KDE Plasma. Gerenciados com GNU Stow.

## Instalação

```bash
curl -fsSL janozeli.github.io/install.sh | bash
```

Ou manualmente:

```bash
git clone https://github.com/janozeli/dotfiles-arch.git ~/dotfiles-arch
cd ~/dotfiles-arch && bash install.sh
```
